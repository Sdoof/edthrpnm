特定の日に探索するOption Chainの作るための手順

1.●opprice-2015-yyyy-mm-dd.csv を秀丸で開く
Excel Fileにcsv形式で正しく読み込む準備をする。
以下の置換を行う。
1,234.00,RUTxxxxx -> 1234.00.RUTxxxxx
  ●秀丸の置換で、
    検索: ^\f(\d)\f,
    置換: \1
       -> 一括置換する。

2. ●UDLY_OPChain.xlsx を開く。
TODAYのワークシートに、
  [ホーム]-[貼り付け]-[テキストファイルウィザード]
で上記●opprice-2015-yyyy-mm-dd.csv内のUDLY部分をCopy & Paste

●Date と TYPE 列を作成
Date 列
 =TODAY()-1
TYPE 列 
 =IF(COUNTIF(B2,"*P*"),1,-1)
と入力してCopy & Paste
* もし、ExpDate列が存在しないか、その値が正しくない場合は、以下の値をCopy & Paste
=TEXT(MID(B2,FIND(1,B2,1),6),"2000!/00!/00")

●UDLY_OPChain_Pre_yyyy-mm-dd.csv
などのcsvファイルに出力して保存

3. ●UDLY_Hist.csv,UDLY_IV.csv を更新する
●UDLY.xlsx内のデータを更新して、それぞれのワークシートからUDLY_Hist.csv,UDLY_IV.csvを出力

4.●UDLY_OPChain_Pre_yyyy-mm-dd.csv を処理対象のファイルとして、●UDLY_OPChain_Pre.csv
  としてcopyする。

EDataProvision2.R(仮称）に●UDLY_OPChain_Pre.csv等を読み込んで、●UDLY_Positions_Pre_yyyy-mm-dd.scv
ファイル出力。このとき、EDataProvision2.R(仮称）内のisSkewCalc=FALSEとすること

